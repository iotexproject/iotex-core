version: 2
jobs:
  build:
    macos:
      xcode: "10.0.0"
    working_directory: /go/src/github.com/iotexproject/iotex-core

    steps:
      - checkout

      - run: brew install golang

      - run: go env
