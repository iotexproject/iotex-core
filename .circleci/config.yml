version: 2
jobs:
  build:
    macos:
      xcode: "10.0.0"
    working_directory: /Users/distiller/project/iotex-core
    steps:
      - checkout

      #- run: brew update
      - run: brew install golang
      - run: ./go.test.sh
      - run: go test -run=XXX -bench=. $(go list ./crypto)
      - run: make minicluster
